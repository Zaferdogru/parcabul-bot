const express = require('express');
const app = express();
const PORT = 3000;

// Basit bir test endpoint
app.get('/', (req, res) => {
  res.send('Parcabul Bot Çalışıyor 🚀');
});

// Sunucuyu dinle
app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});